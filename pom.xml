<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.holon-platform</groupId>
		<artifactId>parent</artifactId>
		<version>1.1.7</version>
	</parent>

	<groupId>com.holon-platform.core</groupId>
	<artifactId>holon-root</artifactId>
	<version>5.5.1</version>
	<packaging>pom</packaging>

	<name>${project.groupId}:${project.artifactId}</name>

	<description>Holon core root pom</description>
	<url>https://holon-platform.com</url>

	<organization>
		<name>The Holon Platform</name>
		<url>https://holon-platform.com</url>
	</organization>

	<licenses>
		<license>
			<name>Apache License Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<developers>
		<developer>
			<name>Holon development team</name>
			<email>dev@holon-platform.com</email>
			<organization>The Holon Platform</organization>
			<organizationUrl>https://holon-platform.com</organizationUrl>
		</developer>
	</developers>

	<issueManagement>
		<url>https://github.com/holon-platform/holon-core/issues</url>
		<system>GitHub Issues</system>
	</issueManagement>

	<scm>
		<connection>scm:git:https://github.com/holon-platform/holon-core.git</connection>
		<url>https://github.com/holon-platform/holon-core</url>
		<developerConnection>scm:git:https://github.com/holon-platform/holon-core.git</developerConnection>
		<tag>v5.5.1</tag>
	</scm>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!-- Annotation api -->
		<annotation-api.version>3.0.0</annotation-api.version>

		<!--Servlet API-->
		<servlet.api>6.1.0</servlet.api>

        <!-- slf4j -->
        <slf4j.version>2.0.17</slf4j.version>

        <!-- Apache commons lang -->
        <commons.lang.version>3.19.0</commons.lang.version>

		<!-- JWT -->
        <jsonwebtoken.version>0.12.6</jsonwebtoken.version>

		<!-- Spring -->
		<spring-framework.version>7.0.6</spring-framework.version>

		<logback-classic.version>1.5.32</logback-classic.version>

		<!-- Spring Security -->
        <spring.security.version>7.0.4</spring.security.version>

		<!-- Spring Boot -->
        <spring.boot.version>4.0.4</spring.boot.version>

		<!-- JavaBeans Validation -->
		<jakarta.validation.api.version>3.1.1</jakarta.validation.api.version>

		<!-- Jackson test/provided -->
        <jackson.databind.version>2.21.1</jackson.databind.version>
        <jackson.jakarta.rs.version>2.21.1</jackson.jakarta.rs.version>

		<!-- JAX-RS API (Jersey 3.x is aligned with JAX-RS 3.1 / Jakarta EE 10) -->
		<ws.rs.api.version>3.1.0</ws.rs.api.version>

		<!-- Test -->
        <jersey.test.version>3.1.10</jersey.test.version>
        <mockito.version>5.20.0</mockito.version>

		<p6spy.version>1.9.1</p6spy.version>
		<hibernate.version>7.2.7.Final</hibernate.version>
		<junit-jupiter.version>6.0.3</junit-jupiter.version>
		<!-- Override parent's separate api/engine version properties to align with 6.0.3 -->
		<junit.jupiter.api.version>6.0.3</junit.jupiter.api.version>
		<junit.jupiter.engine.version>6.0.3</junit.jupiter.engine.version>

		<!-- JAXB runtime (glassfish-jaxb.version=4.0.6 in Spring Boot 4.0.4 BOM) -->
		<jaxb.runtime.version>4.0.6</jaxb.runtime.version>

		<!-- Maven plugin versions -->
		<maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
		<maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>

	</properties>

	<modules>
		<module>core</module>
		<module>http</module>
		<module>auth</module>
		<module>auth-jwt</module>
		<module>async-datastore</module>
		<module>async-http</module>
		<module>spring</module>
		<module>spring-security</module>
		<module>spring-boot</module>
		<module>starter</module>
		<module>starter-security</module>
		<module>starter-test</module>
		<module>test</module>
		<module>bom</module>
		<module>bom-platform</module>
		<module>documentation</module>
	</modules>

	<dependencies>
		<!-- JUnit 5 tests -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-api</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-engine</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Javadoc generation is skipped; Asciidoctor documentation module handles docs -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<phase>none</phase>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.14.0</version>
				<configuration>
					<release>21</release>
				</configuration>
			</plugin>
		</plugins>
		<pluginManagement>
			<plugins>
				<!-- Override Maven default versions inherited from super POM -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-clean-plugin</artifactId>
					<version>${maven-clean-plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-resources-plugin</artifactId>
					<version>${maven-resources-plugin.version}</version>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

</project>